//std::string_view,<string_view> header, replaces [const char*],read only access to strings or substrings without copying memory
//std::span,<span> header,replaces [*T ptr,size_t,len], read/writes access to contiguous sequence of objects(vector or raw array)
//std::views,<ranges> header, replaces [complex iterator loops]
